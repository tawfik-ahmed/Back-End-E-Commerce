export enum UserRole {
  USER = 'user',
  ADMIN = 'admin',
  SUPPLIER = 'supplier',
}

export enum UserGender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum PaymentMethod {
  CASH = 'cash',
  CARD = 'card',
}

export enum RequestProductStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}
